// these APIs are auto-imported from @vueuse/core
const groupedDecimals = (number = 0, max = 2, min = 2): string => number.toLocaleString('en-US', { minimumFractionDigits: min, maximumFractionDigits: max })

export { groupedDecimals }
